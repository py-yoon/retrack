# RE:TRACK 프로젝트 인수인계 및 실행 계획

> 마지막 점검: 2026-08-15  
> 대상: `retrack(GPT)`

## 1. 과업의 목표

RE:TRACK은 서울의 재개발·재건축 등 정비사업 정보를 한곳에 모아, 사용자가 특정 사업장의 **현재 단계와 최근 변동**을 빠르게 파악하도록 돕는 서비스다.

사용자가 얻어야 할 핵심 답은 다음 세 가지다.

1. 내가 찾는 사업장은 현재 어느 단계인가?
2. 최근 어떤 공고·인가·계획 변경이 있었나?
3. 그 변화의 원문과 근거는 무엇인가?

초기 대상 지역은 서울이며, 서울시 공공 데이터와 `정비사업 정보몽땅`을 주요 데이터 원천으로 사용한다.

## 2. 제품 방향과 원칙

- **정확성 우선**: 날짜, 사업장 매칭, 출처를 명확히 보존한다. 매칭이 불확실한 데이터는 노출하지 않거나 검수 가능하게 남긴다.
- **변화 중심**: 단순 사업장 목록보다 ‘오늘/최근 무엇이 바뀌었는가’를 먼저 보여 준다.
- **근거 제시**: 모든 이벤트는 원문 출처 링크를 제공한다.
- **가볍고 빠른 탐색**: 사업장명 또는 주소로 즉시 검색하고, 상세 화면에서 단계와 타임라인을 한눈에 본다.
- **운영 가능한 데이터 파이프라인**: 일회성 적재가 아니라 재실행·중복 방지·실패 확인이 가능한 수집 절차를 만든다.

## 3. 현재 구현 상태

### 완료된 기능

| 영역 | 상태 | 구현 내용 |
| --- | --- | --- |
| 홈 | 완료 | 검색 UI, 최근 변화 카드/목록, Supabase 조회 |
| 사업장 검색 | 완료 | 이름·주소 부분 일치 검색, 상세 페이지 이동 |
| 사업장 상세 | 완료 | 기본 정보, 추진 단계, 이벤트 타임라인 조회 |
| 변화 목록 | 완료 | `/changes`에서 기간·중요도·변화 유형별 이벤트 목록 조회 |
| 출처 확인 | 완료 | 변화 목록과 상세 타임라인에서 원문 링크 제공 |
| 데이터베이스 | 완료 | `projects`, `events`, `project_stages`, `users`, `subscriptions` 스키마와 RLS 정책 |
| 데이터 적재 기반 | 완료 | 서울시 사업장·공고, 정보몽땅 단계 데이터를 SQL로 생성하는 스크립트 |
| 배포 품질 확인 | 완료 | `npm run lint`, `npm run build` 통과 |

### 원격 DB에 확인된 데이터량

2026-08-15 기준 원격 Supabase에 다음 데이터가 적재되어 있다.

- 사업장: **4,068개**
- 이벤트: **592개**
- 추진 단계: **494개**
- 마이그레이션: 로컬의 3개 마이그레이션 모두 원격 적용됨

이 수치는 수집·매칭 로직을 수정하거나 재적재한 뒤 다시 확인해야 한다.

### 아직 미완성 또는 임시인 부분

- 자치구 필터는 아직 제공하지 않는다. 현재 원격 DB의 `projects.district` 값은 4,068건 모두 비어 있어, 수집 단계에서 보강한 뒤 구현해야 한다.
- 변화 목록은 화면에서 20건씩 나누어 표시하지만, 현재는 최대 1,000건을 클라이언트에 불러와 필터링한다. 데이터가 증가하면 DB 쿼리 기반 필터·페이지네이션으로 전환해야 한다.
- 로그인·관심 사업장 구독·알림은 DB 스키마만 있고 사용자 기능은 없다.
- 데이터 생성은 수동 SQL 파일 생성 방식이며, 자동 실행·검수·배포 절차가 없다.
- `README.md`는 Next.js 기본 문서라 프로젝트 운영법이 기록되어 있지 않다.
- 이 폴더 자체에는 Git 저장소가 없다. 작업 이력과 배포 단위를 관리하려면 Git 초기화/원격 연결이 필요하다.

## 4. 기술 구조

```text
서울시 Open API ─────┐
                    ├─ scripts/*.mjs ─→ SQL 파일 ─→ Supabase PostgreSQL
정비사업 정보몽땅 ────┘                                  │
                                                        ├─ projects
Next.js (App Router) ─→ Supabase JS (anon key) ───────────┼─ events
                                                        └─ project_stages
```

### 주요 파일

| 파일 | 역할 |
| --- | --- |
| `src/app/page.tsx` | 홈, 검색, 최근 변화·통계 표시 |
| `src/app/projects/[id]/page.tsx` | 사업장 상세와 타임라인 |
| `src/lib/supabase/client.ts` | Supabase 브라우저/서버 공용 클라이언트 생성 |
| `supabase/migrations/` | DB 스키마 및 식별자 컬럼 변경 이력 |
| `scripts/build-seoul-import.mjs` | 서울시 사업장 데이터를 projects upsert SQL로 생성 |
| `scripts/build-seoul-events-import.mjs` | 서울시 공고를 사업장 이벤트 SQL로 생성 |
| `scripts/build-cleanup-stages-import.mjs` | 정보몽땅 진행 단계를 매칭하여 SQL로 생성 |
| `.env.example` | 필요한 환경 변수 이름의 예시 |

### 환경 변수

로컬 실행에는 다음 값이 `.env.local`에 필요하다. 실제 키는 문서·커밋·클라이언트 로그에 넣지 않는다.

```bash
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SEOUL_OPENAPI_KEY=
```

## 5. 다음 개발 우선순위

### P0 — 실제 데이터 탐색 경험 완성 (진행 중)

가장 먼저 할 일이다. 이미 쌓인 이벤트가 사용자에게 정확히 보이도록 한다.

1. [완료] `/changes` 전체 변화 목록 페이지를 만들었다.
2. [진행 중] 날짜 범위(오늘, 7일, 30일, 전체), 중요도, 변화 유형, 사업 유형, 현재 단계 필터와 20건 단위 화면 페이지네이션을 추가했다. 자치구 필터와 직접 날짜 지정은 남아 있다.
3. [완료] 홈의 ‘오늘의 변화’와 ‘최근 변화’를 실제 날짜 기준 쿼리로 변경했다.
4. [완료] 고정 날짜를 서울 시간대의 현재 날짜로 교체했다.
5. [완료] 목록과 상세 타임라인에 `source_name`, 외부 원문 링크(`source_url`)를 표시했다.
6. [완료] 빈 결과·로딩·연결 오류 상태를 명확히 표시한다.

**완료 기준**

- 홈의 수치와 목록이 동일한 날짜 조건의 실제 DB 결과와 일치한다.
- 전체 보기에서 필터된 이벤트 목록으로 이동한다.
- 이벤트마다 출처와 원문으로 이동하는 링크가 있다.
- DB 오류와 ‘결과 없음’을 사용자가 구분할 수 있다.

### P1 — 데이터 품질과 수집 운영화

1. `package.json`에 데이터 스크립트 실행 명령을 추가한다.
2. SQL 생성 → 원격 DB 반영 → 결과 건수 확인을 문서화하고 하나의 재현 가능한 절차로 만든다.
3. 이름/주소 기반 사업장 매칭 결과를 검수한다. 특히 잘못 붙은 이벤트와 단계의 표본을 확인한다.
4. 매칭 신뢰도 또는 매칭 방식(정확 일치/주소 일치/추정)을 저장할지 결정한다.
5. 수집 실행 시 원본 건수, 매칭 건수, 미매칭 건수, 오류를 남긴다.
6. 정기 수집 주기(예: 매일 1회)와 실패 알림 방식을 결정하고 자동화한다.

**완료 기준**

- 새 수집을 실행해도 `source_code`, `source_event_id` 기준으로 중복이 생기지 않는다.
- 실행 후 데이터 건수와 미매칭 수를 확인할 수 있다.
- 표본 검수에서 잘못된 사업장 매칭을 수정할 수 있는 절차가 있다.

### P2 — 개인화 기능

DB에 이미 있는 `users`, `subscriptions`를 활용한다.

1. Supabase Auth 로그인/로그아웃을 추가한다.
2. 상세 화면에 관심 사업장 추가·해제 버튼을 구현한다.
3. 관심 사업장의 새 이벤트를 모은 ‘내 변화’ 화면을 만든다.
4. 이메일 또는 푸시 알림을 도입할 경우, 수신 동의·빈도·해지 흐름을 함께 설계한다.

**완료 기준**

- 사용자는 본인의 관심 사업장만 조회·관리할 수 있다.
- 신규 이벤트가 생기면 관심 사업장 화면에 일관되게 반영된다.

### P3 — 신뢰성과 서비스 운영

1. 프로젝트 README를 이 문서의 실행 요약과 함께 갱신한다.
2. Git 저장소를 초기화하고 원격 저장소·브랜치·배포 절차를 정한다.
3. E2E 테스트(검색, 상세, 필터, 출처 링크)와 수집 스크립트 단위 테스트를 추가한다.
4. 접근성(키보드 검색, 링크 레이블, 색상 외 중요도 표기)과 모바일 화면을 점검한다.
5. Supabase 키 관리, RLS 정책, 데이터 백업·오류 모니터링을 점검한다.

## 6. 권장 작업 순서

다음 순서로 진행하면 사용자 가치와 운영 안정성을 함께 확보할 수 있다.

1. P0의 전체 변화 목록, 홈 날짜 필터, 출처 링크 구현
2. 실제 데이터로 검색·상세·필터 수동 검수
3. P1의 수집 명령·실행 로그·매칭 품질 점검
4. 수집 자동화 및 배포 절차 문서화
5. P2의 로그인과 관심 사업장 기능

P2 알림 기능은 P0/P1에서 데이터의 최신성·출처·중복 방지가 검증된 뒤 시작한다.

## 7. 다른 AI/개발자가 작업할 때의 규칙

- 구현 전에는 이 문서와 해당 화면·마이그레이션·수집 스크립트를 함께 읽는다.
- 스키마 변경은 반드시 새 파일을 `supabase/migrations/`에 추가한다. 기존 적용 마이그레이션을 수정하지 않는다.
- 외부 데이터 적재는 식별자 기반 upsert와 중복 방지를 유지한다.
- 데이터 출처 URL과 원본 식별자를 버리지 않는다.
- 비밀값은 `.env.local`에만 두며 Git에 커밋하지 않는다.
- 기능 추가 후 최소 `npm run lint`와 `npm run build`를 실행한다.
- 사용자에게 숫자·날짜·‘최근’ 같은 표현을 보여 줄 때는 쿼리의 실제 조건과 일치시킨다.
- 공고와 사업장 자동 매칭은 틀릴 수 있으므로, 확실하지 않은 매칭을 사실처럼 표시하지 않는다.

## 8. 빠른 시작

```bash
npm install
cp .env.example .env.local
# .env.local에 실제 Supabase / 서울 Open API 값을 입력
npm run dev
```

검증:

```bash
npm run lint
npm run build
```

원격 Supabase 마이그레이션 상태 확인:

```bash
npx supabase migration list
```

## 9. 작업 이력 — 2026-08-15

아래 항목은 계획이 아니라, 이 작업 폴더에서 구현·검증까지 완료된 변경이다.

### 사전 점검 완료

- 원격 Supabase 마이그레이션 3개가 로컬과 동일하게 적용된 것을 확인했다.
- 원격 데이터량을 확인했다: 사업장 4,068개, 이벤트 592개, 추진 단계 494개.
- `npm run lint`와 `npm run build`가 기존 코드에서 통과하는 것을 확인했다.

### P0 탐색 기능 구현 완료

| 변경 | 구현 내용 | 관련 파일 |
| --- | --- | --- |
| 홈의 실제 데이터화 | 고정 예시/고정 날짜를 제거하고, 서울 시간대 기준 ‘오늘’ 통계와 최근 7일 이벤트를 조회한다. 로딩·빈 결과·연결 오류도 표시한다. | `src/app/page.tsx` |
| 전체 변화 목록 | `/changes` 페이지를 새로 만들고 전체 이벤트를 시간 역순으로 표시한다. | `src/app/changes/page.tsx` |
| 필터 | 기간(오늘/7일/30일/전체), 중요도, 변화 유형, 사업 유형, 현재 단계를 조합해 필터링할 수 있다. | `src/app/changes/page.tsx` |
| 페이지 이동 | 변화 목록은 화면당 20건씩 이전/다음으로 나누어 표시한다. | `src/app/changes/page.tsx` |
| 화면 연결 | 홈의 ‘전체 보기’와 ‘최근 7일 전체’에서 `/changes`로 이동한다. | `src/app/page.tsx` |
| 원문 근거 | 전체 목록과 사업장 상세 타임라인에 `source_name`과 `source_url` 기반 ‘원문 보기’ 링크를 추가했다. | `src/app/changes/page.tsx`, `src/app/projects/[id]/page.tsx` |

### 데이터 조사 결과와 제한 사항

- `projects.project_type`은 4,067개 사업장에 존재하므로 현재 필터에 사용했다.
- `projects.current_status`는 정보몽땅 단계 적재 결과를 사용하므로 현재 단계 필터에 사용했다.
- `projects.district`는 원격 DB의 4,068개 사업장 모두 비어 있다. 따라서 자치구 필터는 UI만 먼저 만들지 않았으며, 수집 단계에서 값을 채운 뒤 구현해야 한다.
- `/changes`는 현재 이벤트가 592건이라 최대 1,000건을 클라이언트에서 받아 필터링한다. 화면상 페이지네이션은 있지만, 데이터가 커지면 Supabase 쿼리 자체에 필터와 범위 조회를 적용하는 서버/DB 기반 페이지네이션으로 교체해야 한다.

### 최종 검증 결과

가장 최근 코드 기준으로 다음 명령이 모두 성공했다.

```bash
npm run lint
npm run build
```

빌드에 확인된 라우트는 `/`, `/changes`, `/projects/[id]`다. 이 작업에서는 스키마·원격 데이터·환경 변수 값을 변경하지 않았고, 화면 코드와 이 문서만 변경했다.

## 10. 바로 이어서 할 일

1. **자치구 데이터 보강**: 서울시 원본의 주소/행정구 정보를 확인해 `projects.district`를 채우는 수집 로직과 마이그레이션/재적재 절차를 만든다.
2. **변화 목록의 DB 기반 페이지네이션**: 현재의 1,000건 클라이언트 조회를 조건별 Supabase 쿼리와 `range` 기반 조회로 바꾼다.
3. **직접 날짜 지정**: 시작일·종료일 필터를 추가하고, 서울 시간대 및 빈 날짜 범위 처리를 검증한다.
4. **P1 데이터 품질 운영화**: 수집 명령, 매칭률/미매칭 로그, 표본 검수, 정기 실행을 만든다.

**다음 구현은 자치구 필터 UI부터 만들지 말고, 먼저 정확한 `district` 데이터를 수집·검증하는 것부터 시작한다.**
